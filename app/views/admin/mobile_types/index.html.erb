<h1>Listing mobile_types</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mobile_types.each do |mobile_type| %>
  <tr>
    <td><%= mobile_type.title %></td>
    <td><%= link_to 'Show', mobile_type %></td>
    <td><%= link_to 'Edit', edit_mobile_type_path(mobile_type) %></td>
    <td><%= link_to 'Destroy', mobile_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mobile type', new_mobile_type_path %>
