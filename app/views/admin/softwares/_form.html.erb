<%= form_for([:admin, @admin_software]) do |f| %>
  <% if @admin_software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_software.errors.count, "error") %> prohibited this admin_software from being saved:</h2>

      <ul>
      <% @admin_software.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :language %><br />
    <%= f.text_field :language %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
